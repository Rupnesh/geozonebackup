import { connect } from 'react-redux';

import SuperPoleView from './SuperPoleView';

const mapStateToProps = (state) => {
    return {};
};

export default connect(mapStateToProps, {})(SuperPoleView);